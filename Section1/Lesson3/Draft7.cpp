#include <iostream>

using namespace std;

int main()
{
    //in C++ language, unlike C language, a string constant is pointed by a const char* pointer
    //in C language, a string constant is pointed by a char* pointer

    const char* name = "Taha Mert Gökdemir";
    char* wife_name = "Buse Gökdemir";

    return 0;
}